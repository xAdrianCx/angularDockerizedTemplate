#build
    docker build -t angular .

    docker run -itd -v ${PWD}:/app --name angularjwt angular
    docker exec -it  angularjwt ng new taskJWT --directory=.
    sudo chown -R $USER:$(id -gn $USER) ./*

#tras prueba, eliminar container e imagen
    docker rm -fv angularjwt
    docker system prune -a

#Levantar ahora el compose
    docker-compose up

#correr comandos, estando dentro de la carpeta del compose y con este inicado
    docker-compose exec web ng g c xcy
